<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zoro175.tajwid"
    android:installLocation="auto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayPengenalanActivity" />
        <activity android:name=".DisplayKonsepActivity" />
        <activity android:name=".DisplayNunActivity" />
        <activity android:name=".DisplayIzharActivity" />
        <activity android:name=".DisplayIzharContoh1" />
        <activity android:name=".DisplayMimActivity" />
        <activity android:name=".DisplayIzharContoh" />
        <activity android:name=".DisplayKreditActivity"></activity>
    </application>

</manifest>